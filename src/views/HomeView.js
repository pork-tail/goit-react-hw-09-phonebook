import React from "react";
import styles from "./Views.Module.css";

const HomeView = () => (
  <div className={styles.container}>
    <h1 className={styles.heroTitle}>Phonebook</h1>
  </div>
);

export default HomeView;
